#!/bin/zsh

set -euo pipefail

usage() {
  echo "Usage: dotfiles sync"
}

case "${1:-}" in
  sync)
    exec bash "$HOME/dotfiles/install.sh"
    ;;
  ""|-h|--help|help)
    usage
    ;;
  *)
    usage
    exit 1
    ;;
esac
