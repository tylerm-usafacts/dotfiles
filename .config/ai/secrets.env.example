# Copy to ~/.config/ai/secrets.env and replace the placeholder.
# Keep this file untracked and restrict it to the current user: chmod 600 ~/.config/ai/secrets.env
export GITHUB_PERSONAL_ACCESS_TOKEN="replace-with-fine-grained-read-only-pat"
