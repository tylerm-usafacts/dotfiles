# Tool Usage Policy

Use this policy when selecting tooling during execution.

- Prefer dedicated read/search/edit tooling over shell equivalents when available in the runtime.
- Use shell commands for terminal operations such as git, tests, builds, and package tooling.
- For broad discovery, use specialized exploration tooling when available; for targeted lookups, use direct grep or glob.
